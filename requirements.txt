# PyTorch is resolved by pip for whatever platform it runs on:
#   linux aarch64 (Oracle Ampere)  -> CPU wheels, which is what a server wants
#   windows / linux x86_64         -> CPU wheels from PyPI
# Do not pin a "+cuXXX" build here: those wheels only exist on PyTorch's own
# index for x86, so pinning one makes the install fail everywhere else.
#
# For a Pascal GPU (GTX 10xx) install the CUDA build explicitly instead, because
# PyTorch builds for cu128+ dropped Pascal support:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
torch
torchvision
pillow
numpy
pandas
matplotlib
ultralytics
