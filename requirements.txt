# Install with the PyTorch cu126 index (cu126 is REQUIRED — cu128+ builds no
# longer support Pascal GPUs such as the GTX 1050 Ti):
#   python -m venv .venv
#   .venv/bin/pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
torch==2.14.0+cu126
torchvision==0.29.0+cu126
pillow
numpy
pandas
matplotlib
ultralytics
